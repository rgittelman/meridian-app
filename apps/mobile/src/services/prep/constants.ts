/** Plan / Life / detail prep copy — disabled until Event Prep Intelligence ships. */
export const PREP_UI_SURFACING_ENABLED = false;

/** Focus PrepAwarenessSection — uses the same cluster pipeline, separate surface. */
export const PREP_FOCUS_AWARENESS_ENABLED = true;
