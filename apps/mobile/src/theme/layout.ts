import { spacing } from './spacing';

/** Horizontal screen padding — consistent across tabs */
export const screenPaddingHorizontal = spacing[5];

/** Top rhythm below safe area on tab screens */
export const screenPaddingTop = spacing[6];

/** Minimum touch target (accessibility) */
export const minTouchTarget = 44;

/** Tab bar float inset from screen bottom */
export const tabBarBottomInset = spacing[2];

/** Tab bar height excluding safe area */
export const tabBarHeight = 62;
